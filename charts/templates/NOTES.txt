The {{ .Values.environment }} environment has been rendered in namespace {{ .Release.Namespace }}.
API services:
{{- range $name, $service := .Values.services }}
{{- if $service.enabled }}
  {{ $name }}: {{ include "fastapiapp.serviceName" (dict "root" $ "name" $name) }}:{{ $service.port }}
{{- end }}
{{- end }}
